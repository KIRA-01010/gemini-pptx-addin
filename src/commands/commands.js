// This file is required as the manifest's FunctionFile target.
// The button is wired as a ShowTaskpane action, so no function actions
// are registered here today, but Office.js still expects the file to
// call Office.onReady / Office.actions.associate if you add ExecuteFunction
// buttons later.

Office.onReady(() => {
  // Reserved for future ribbon actions (e.g. a "Quick generate" button
  // that runs without opening the task pane).
});
